
package an.improved.math.learning.tools;
import java.util.Scanner;
public class AnImprovedMathLearningTools {

  
    public static void main(String[] args) {
       Scanner input = new Scanner(System.in);
       int num1 = (int)(Math.random() * 10);
       int num2 = (int)(Math.random() * 10);
       if(num1>=num2){
           System.out.println("What is the result of " + num1 + " - " + num2 + " = ?");
           int userresult = input.nextInt();
           int programresult = num1 - num2;
           
           if(userresult == programresult){
               System.out.println("Your awnser is corect!");
           } else {
               System.out.println("Your awnser is incorect!");
           }
       }
    }
    
}
